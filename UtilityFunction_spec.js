const supertest = require("supertest")
const config = require('./config').config;
const cardBaseUrl = supertest(config.cardBaseUrl);

var UtilityFunction = function () {

    
       
}
module.exports = new UtilityFunction();
